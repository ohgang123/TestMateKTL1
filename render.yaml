services:
  - type: web
    name: ktl-testmate
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: bash ./start.sh
    healthCheckPath: /api/dashboard
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
